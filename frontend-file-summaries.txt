{
  "frontend/index.html": "HTML entry point with updated meta tags (og:title, twitter:title, description) referencing \"US Finance Helps\" and Google Fonts preconnect for Inter and Merriweather.",
  "frontend/src/index.css": "Global CSS with a pastel OKLCH color system replacing the previous green theme — uses soft lavender as primary, mint as secondary, peach as accent, and introduces pastel-* and brand-* custom tokens for both light and dark modes.",
  "frontend/tailwind.config.js": "Extended Tailwind config updated to reference the new pastel CSS tokens, adding `pastel.*` and `brand.*` color groups, updated card shadows, and keeping all existing font/animation/radius settings.",
  "frontend/src/App.tsx": "Main app with TanStack Router setup for '/' and '/category/$category' routes, now wrapped with ErrorBoundary and LeadCaptureProvider so lead capture state is available throughout the app and rendering errors are caught gracefully.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations: getAllResources, getResourcesByCategory, getResource, addResource, and deleteResource with proper cache invalidation.",
  "frontend/src/components/Header.tsx": "Sticky header updated to use `brand-deep` color token instead of the old `finance-green-deep`, keeping all navigation and mobile menu functionality intact.",
  "frontend/src/components/Footer.tsx": "Footer updated to use `brand-deep` color token and `pastel-rose` heart icon, with an updated disclaimer mentioning sponsored offers.",
  "frontend/src/components/TopicCard.tsx": "Topic card updated to use `pastel-lavender` background for the icon container instead of the old green pale token.",
  "frontend/src/components/ResourceCard.tsx": "Resource card updated to use `pastel-mint` background for the book icon container instead of the old green pale token.",
  "frontend/src/pages/HomePage.tsx": "Homepage that renders `LeadCaptureModal` when `isLeadCaptured` is false, and includes the `TopLoanOffersBanner`, topic grid, reader reviews, and resource sections.",
  "frontend/src/pages/CategoryPage.tsx": "Category page that renders `LeadCaptureModal` when `isLeadCaptured` is false, and includes the discussion section, gated loan offers section, resource search, and sidebar with a lock prompt when offers are not yet unlocked.",
  "frontend/src/data/reviewsData.ts": "Static data file containing editorial discussion paragraphs and 3 user reviews (with star ratings, text, and reviewer names) for each of the 7 finance categories.",
  "frontend/src/data/loanOffersData.ts": "Static data file with 2–3 sponsored loan offer objects per finance category plus 6 top homepage banner offers, with all lender names updated to real brand names: \"24/7 Credit Loan (up to $5,000)\" for leadfinv c=294173, \"Wizzay (up to $5,000)\" for leadfinv c=373383, \"Hot Loans Today\", \"Honest Loans\", \"NextDayPersonalLoan\" for wedebeek offer_id=20435, and \"EverdayLoans\" for wedebeek offer_id=20370. All affiliate URLs and token placeholders for hotloanstoday.com and honestloans.net are preserved unchanged.",
  "frontend/src/components/ReviewCard.tsx": "Reusable card component displaying a user review with a 5-star rating row, review quote text, and reviewer avatar initial and name using pastel styling.",
  "frontend/src/components/DiscussionSection.tsx": "Section component that renders a category-specific editorial discussion paragraph in a pastel lavender callout box followed by a grid of ReviewCard components.",
  "frontend/src/components/LoanOfferCard.tsx": "Loan offer card that consumes `LeadCaptureContext`; renders a blurred placeholder with a lock icon when the lead form hasn't been submitted, and the full card with a dynamically token-substituted affiliate URL when it has.",
  "frontend/src/components/LoanOffersSection.tsx": "Section component rendering a grid of `LoanOfferCard` components for a given category; shows an informational lock banner above the cards when the lead form hasn't been submitted yet.",
  "frontend/src/components/TopLoanOffersBanner.tsx": "Homepage banner that consumes LeadCaptureContext; shows blurred placeholder cards with a lock overlay when the lead form is pending, and after submission renders all 6 affiliate offer cards in a 3-column grid (2 on sm) with token-substituted URLs via buildAffiliateUrl.",
  "frontend/src/context/LeadCaptureContext.tsx": "React context and provider that manages lead capture form state (7 user fields), exposes `isLeadCaptured`, `leadData`, and `submitLead`, and persists submitted data in `sessionStorage` so it survives page navigation within the same session.",
  "frontend/src/utils/affiliateUrlBuilder.ts": "Utility function that replaces `[firstName]`, `[lastName]`, and `[email]` token placeholders in affiliate URL strings with URL-encoded values from the submitted lead form data.",
  "frontend/src/components/LeadCaptureModal.tsx": "Full-screen modal component with a 7-field lead capture form (First Name, Last Name, Email, Phone, Street Address, City, ZIP); validates all fields are non-empty, shows a loading state on submit, then calls `submitLead` from `LeadCaptureContext` to unlock offers.",
  "frontend/src/main.tsx": "Application entry point that wraps the app with `QueryClientProvider`, `InternetIdentityProvider`, and the new `LeadCaptureProvider` so lead capture state is available throughout the entire app.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based ErrorBoundary that catches rendering errors anywhere in the component tree and displays a user-friendly fallback UI with a refresh button, styled consistently with the app's pastel finance theme."
}